# Production default: the admin nginx container proxies /api to the API service.
VITE_API_BASE_URL=/api

# Development only: Vite proxies /api to this address.
VITE_API_PROXY_TARGET=http://localhost:4001
